## Settings file was created by plugin HammerTime v0.3.3
## Plugin GUID: com.maxsch.valheim.HammerTime

[1 - General]

## Disables crafting recipes of custom hammers that are enabled in this mod. Only deactivates the recipes, existing items will not be removed. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Disable Hammer Recipes = true

[Better Wards _HammerPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Enable Hammer = false

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Combine Categories = true

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Better Wards
Combined Category Name = Better Wards

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Better Wards Misc
Category Name Misc = Better Wards Misc

[BuildIt _RKCustomTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Enable Hammer = true

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Combine Categories = false

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: BuildIt
Combined Category Name = BuildIt

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: BuildIt Roofs
Category Name Roofs = BuildIt Roofs

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: BuildIt Structure
Category Name Structure = BuildIt Structure

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: BuildIt Furniture
Category Name Furniture = BuildIt Furniture

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: BuildIt Outdoors
Category Name Outdoors = BuildIt Outdoors

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: BuildIt Greenhouse
Category Name Greenhouse = BuildIt Greenhouse

[Clutter _ClutterPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Enable Hammer = true

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Combine Categories = false

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Clutter
Combined Category Name = Clutter

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Clutter Decor
Category Name Decor = Clutter Decor

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Clutter Amenities
Category Name Amenities = Clutter Amenities

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Clutter Kitchen
Category Name Kitchen = Clutter Kitchen

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Clutter Sculptures
Category Name Sculptures = Clutter Sculptures

[Epic Loot _HammerPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Enable Hammer = false

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Combine Categories = true

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Epic Loot
Combined Category Name = Epic Loot

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Epic Loot Crafting
Category Name Crafting = Epic Loot Crafting

[Epic Valheims Additions - by Huntard _HammerPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Enable Hammer = false

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Combine Categories = true

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Epic Valheims Additions - by Huntard
Combined Category Name = Epic Valheims Additions - by Huntard

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Epic Valheims Additions - by Huntard Crafting
Category Name Crafting = Epic Valheims Additions - by Huntard Crafting

[Item Drawers _HammerPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Enable Hammer = false

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Combine Categories = true

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Item Drawers
Combined Category Name = Item Drawers

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Item Drawers Furniture
Category Name Furniture = Item Drawers Furniture

[More Gates _HammerPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Enable Hammer = false

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Combine Categories = true

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: More Gates
Combined Category Name = More Gates

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: More Gates MoreGates
Category Name MoreGates = More Gates MoreGates

[OdinArchitect _HammerPieceTable2]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Enable Hammer = true

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Combine Categories = false

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: OdinArchitect
Combined Category Name = OdinArchitect

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: OdinArchitect Misc
Category Name Misc = OdinArchitect Misc

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: OdinArchitect Pieces
Category Name Pieces = OdinArchitect Pieces

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: OdinArchitect Doors
Category Name Doors = OdinArchitect Doors

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: OdinArchitect Decors
Category Name Decors = OdinArchitect Decors

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: OdinArchitect Big
Category Name Big = OdinArchitect Big

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: OdinArchitect Dvergr
Category Name Dvergr = OdinArchitect Dvergr

[OdinCampsite _HammerPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Enable Hammer = false

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Combine Categories = true

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: OdinCampsite
Combined Category Name = OdinCampsite

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: OdinCampsite Misc
Category Name Misc = OdinCampsite Misc

[OdinOnDemand _HammerPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Enable Hammer = false

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Combine Categories = true

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: OdinOnDemand
Combined Category Name = OdinOnDemand

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: OdinOnDemand OOD
Category Name OOD = OdinOnDemand OOD

[OdinsBarrelRide _HammerPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Enable Hammer = false

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Combine Categories = true

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: OdinsBarrelRide
Combined Category Name = OdinsBarrelRide

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: OdinsBarrelRide Misc
Category Name Misc = OdinsBarrelRide Misc

[OdinsTraps _HammerPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Enable Hammer = false

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Combine Categories = true

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: OdinsTraps
Combined Category Name = OdinsTraps

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: OdinsTraps Misc
Category Name Misc = OdinsTraps Misc

[Pressure-Plate _HammerPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Enable Hammer = false

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Combine Categories = true

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Pressure-Plate
Combined Category Name = Pressure-Plate

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Pressure-Plate Misc
Category Name Misc = Pressure-Plate Misc

[SleepingBag _HammerPieceTable]

Enable Hammer = false

Combine Categories = true

Combined Category Name = SleepingBag

Category Name Furniture = SleepingBag Furniture

[ValheimRAFT _HammerPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Enable Hammer = false

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Combine Categories = true

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: ValheimRAFT
Combined Category Name = ValheimRAFT

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: ValheimRAFT ValheimRAFT
Category Name ValheimRAFT = ValheimRAFT ValheimRAFT

[Vanilla _HammerPieceTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Enable Hammer = false

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Combine Categories = false

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Vanilla
Combined Category Name = Vanilla

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Misc
Category Name Misc = Misc

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Crafting
Category Name Crafting = Crafting

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Building
Category Name Building = Building

## Used category name if categories are not combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Furniture
Category Name Furniture = Furniture

[Vanilla _HammerPieceTable2]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Enable Hammer = true

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Combine Categories = false

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Vanilla
Combined Category Name = Vanilla

[Vanilla _RKCustomTable]

## Enables moving pieces from this custom hammer into the vanilla hammer. Can be changed at runtime.
# Setting type: Boolean
# Default value: true
Enable Hammer = true

## Combines all categories from this custom hammer into one category. Can be changed at runtime.
# Setting type: Boolean
# Default value: false
Combine Categories = false

## Used category name if categories are combined. A new category is getting created if it is not a vanilla value. If two categories have the same name the pieces will be combined, even from different hammers. Can be changed at runtime.
# Setting type: String
# Default value: Vanilla
Combined Category Name = Vanilla

